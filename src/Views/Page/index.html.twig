{% extends "template.html.twig" %}

{% block body %}
    <h1>Bienvenue !</h1>

    {% for movie in movies %}
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ movie.title }}</h5>
                <p class="card-text">{{ movie.synopsis }}</p>
                <a href="{{ baseUrl }}/movie/show/{{ movie.id }}" class="btn btn-primary">Voir ce film</a>
                <a href="{{ baseUrl }}/movie/edit/{{ movie.id }}" class="btn btn-primary">Éditer ce film</a>
            </div>
        </div>
    {% endfor %}

{% endblock %}